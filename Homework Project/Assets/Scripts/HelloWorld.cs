using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewBehaviourScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Hello, world!");
        Debug.LogWarning("Are you sure you want to do that?");
        Debug.LogError("You shouldn't have done that.");
        // Author: Guadalupe Lopez-Siles 
        /*Will we have to model or render our own objects to use in our games?
         * My game idea would be a person having to escap from a mall decorated like the 1970s, '80s and '90s
         * The development question I had was how long does it take to knock out bugs and glitches during game development time?
         * 
         * 
         */
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
        